#include <stdio.h>

int main() {
    int n, i, num = 1;

    printf("Enter the number of terms: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
        printf("%d ", num);
        num = num + 2; // add 2 to the previous number to get the next number in the series
    }

    return 0;
}
