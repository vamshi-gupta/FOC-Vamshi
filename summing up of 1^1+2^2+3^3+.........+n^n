#include <stdio.h>
#include <math.h>

int main() {
    int n;
    printf("Enter the value of n: ");
    scanf("%d", &n);

    long sum = 0;
    for (int i = 1; i <= n; i++) {
        long term = pow(i, i);
        sum += term;
    }

    printf("Sum of the series: %ld\n", sum);

    return 0;
}
