#include <stdio.h>

int main() {
    int limit, i, sum;

    printf("Enter a limit: ");
    scanf("%d", &limit);

    sum = 0;

    for (i = 1; i <= limit; i += 2) {
        sum += i * i;
    }

    printf("The sum of the squares of odd numbers up to %d is %d\n", limit, sum);

    return 0;
}
