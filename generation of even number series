#include <stdio.h>

int main() {
    int n, i, num=0;
    
    printf("Enter the value of n: ");
    scanf("%d", &n);
    
    printf("Even number series of %d numbers: ", n);
    for(i=1; i<=n; i++) {
        printf("%d ", num);
        num += 2;
    }
    
    return 0;
}
